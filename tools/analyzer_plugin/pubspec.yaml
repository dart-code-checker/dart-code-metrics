name: dart_code_metrics_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 4.15.1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  dart_code_metrics:
    path: /Users/dmitry/Development/dart/dart-code-metrics

dev_dependencies:
  lints: ^1.0.1
